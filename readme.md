# pacsync

`pacsync` is a tool to sync (add & remove) a package list file with the system

`pacupgrade` is a tool to upgrade the system and sync to a package list file

these allow you to manage your arch system from a package list file (declaratively) without touching pacman (imperatively)
